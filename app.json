{
  "name": "SQLatch",
  "description": "BatiBots © IoT Lviv",
  "image": "",
  "repository": "https://bitbucket.org/batibots/api.git",
  "keywords": ["python"],
  "addons": [ "heroku-postgresql" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "main": {
      "scripts": {
        "run": "python bot/xo.py"
      }
    },
    "test": {
      "scripts": {
        "test": "python setup.py test",
      }
    }
  }
}
